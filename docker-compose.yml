services:
  db:
    image: postgres:15
    container_name: banking-db
    environment:
      POSTGRES_DB: banking_middleware
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
